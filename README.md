# pdf-gen

This package now supports rendering text with accented characters such as `é` or
`à`. Internally the PDF renderer converts UTF-8 strings to the encoding used by
FPDF so these characters appear correctly in the resulting document.
 
